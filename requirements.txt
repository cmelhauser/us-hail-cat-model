# CONUS Hail Cat Model v2.0
# Python 3.9+ required
# Install: pip install -r requirements.txt

# Core scientific
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Geospatial
rasterio>=1.3
xarray>=2023.1
regionmask>=0.11
cartopy>=0.22

# L-moments (CDF fitting)
lmoments3>=1.0.4

# Parquet I/O (stochastic catalog)
pyarrow>=12.0

# Visualisation
matplotlib>=3.7

# Radar data access (MYRORSS / MRMS)
s3fs>=2023.1
boto3>=1.26
cfgrib>=0.9.10
eccodes>=1.5
netCDF4>=1.6
h5py>=3.8

# ERA5 access (freezing levels)
cdsapi>=0.7

# Processing and analysis
scikit-learn>=1.2
tqdm>=4.65
requests>=2.28
